<!DOCTYPE html>
<html lang="en" 
      xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{admin/layout}">
<head>
    <title layout:title-fragment="title">Quản lý Categories</title>
</head>
<body>
    <div layout:fragment="content">
        <h1>Quản lý Categories</h1>

        <a th:href="@{/admin/categories/new}" class="btn btn-add">Thêm Category Mới</a>
        <p th:if="${message}" class="success" th:text="${message}"></p>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Tên Category</th>
                    <th>Mô tả</th>
                    <th>Thao tác</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="cat : ${categories}">
                    <td th:text="${cat.id}">1</td>
                    <td th:text="${cat.name}">Yoga</td>
                    <td th:text="${cat.description}">Video về Yoga</td>
                    <td>
                        <a th:href="@{'/admin/categories/edit/' + ${cat.id}}" class="btn btn-edit">Sửa</a>
                        <a th:href="@{'/admin/categories/delete/' + ${cat.id}}" class="btn btn-delete"
                           th:onclick="|return confirm('Bạn có chắc chắn muốn xóa Category ID ${cat.id}?');|">Xóa</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>