:root{
  --bg:#f6f0ef;
  --card:#fff;
  --muted:#8b8b8b;
  --accent:#d67b8f;
}

body {
  font-family: "Segoe UI", Roboto, Arial, sans-serif;
  background: linear-gradient(135deg,#fdf6f0,var(--bg));
  margin:0;
  color:#333;
}

.container { width: 900px; margin: 20px auto; }

.topbar {
  background: rgba(255,255,255,0.6);
  padding:12px 0;
  border-bottom:1px solid rgba(0,0,0,0.05);
}
.topbar .container { display:flex; justify-content:space-between; align-items:center; }
.brand { font-weight:700; font-size:20px; color: #333; text-decoration:none; }
nav a { margin-left:10px; color:#333; text-decoration:none; }

.feed { margin-top:20px; }
.post-card {
  background: var(--card);
  border-radius:12px;
  box-shadow: 0 6px 18px rgba(21,21,21,0.04);
  padding:12px;
  margin-bottom:18px;
}
.post-header { display:flex; justify-content:space-between; font-size:14px; color:var(--muted); }
.post-image img { width:100%; border-radius:8px; margin-top:8px; }
.post-caption { padding:10px 0; font-size:15px; color:#222; }

.auth { background:var(--card); padding:20px; border-radius:10px; max-width:420px; margin:20px auto; box-shadow: 0 6px 18px rgba(0,0,0,0.03); }
.auth label { display:block; margin-bottom:8px; }
.auth input, .auth textarea { width:100%; padding:8px; border-radius:6px; border:1px solid #e6e6e6; margin-top:6px; }
button { background:var(--accent); color:white; padding:10px 14px; border:none; border-radius:8px; cursor:pointer; }
button:hover { transform:translateY(-1px); }

.footer { text-align:center; padding:20px 0; color:var(--muted); margin-top:20px; }
.error { color:#b00020; margin-bottom:8px; }
