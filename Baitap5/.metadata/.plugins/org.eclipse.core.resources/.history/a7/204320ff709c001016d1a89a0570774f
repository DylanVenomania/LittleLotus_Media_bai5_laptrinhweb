<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>[[${pageTitle}]]</title>
    <style> /* CSS đồng bộ */ </style>
</head>
<body>
    <h1>[[${pageTitle}]]</h1>

    <form th:action="@{/admin/users/save}" method="post" th:object="${user}">
        <input type="hidden" th:field="*{id}" />

        <div>
            <label>Email:</label>
            <input type="text" th:field="*{email}" required />
        </div>
        
        <div>
            <label>Password:</label>
            <input type="text" th:field="*{password}" th:required="${user.id == null}" 
                   placeholder="Nhập mật khẩu (bắt buộc cho Thêm mới)" />
        </div>

        <div>
            <label>Username:</label>
            <input type="text" th:field="*{username}" required />
        </div>
        
        <div>
            <label>Role:</label>
            <select th:field="*{role}" required>
                <option value="">-- Chọn Role --</option>
                <option value="USER">USER</option>
                <option value="ADMIN">ADMIN</option>
            </select>
        </div>

        <button type="submit" class="btn-submit">Lưu User</button>
        <a th:href="@{/admin/users}" class="btn-cancel">Hủy</a>
    </form>
</body>
</html>