<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>LittleLotus Feed</title>
    <link rel="stylesheet" th:href="@{/css/style.css}">
</head>
<body>
    <header th:replace="fragments/header :: header"></header>

    <main>
        <h1>Feed</h1>
        <div th:if="${posts.isEmpty()}">
            <p>Chưa có bài viết nào.</p>
        </div>
        <div th:each="post : ${posts}" class="post">
            <h3 th:text="${post.user.displayName}">User Name</h3>
            <img th:src="${post.imageUrl}" alt="Post Image" width="300">
            <p th:text="${post.caption}">Caption</p>
            <p>Created at: <span th:text="${post.createdAt}">Date</span></p>

            <div class="comments">
                <h4>Comments</h4>
                <div th:if="${post.comments.isEmpty()}">
                    <p>Chưa có bình luận.</p>
                </div>
                <ul>
                    <li th:each="comment : ${post.comments}">
                        <strong th:text="${comment.user.displayName}">Commenter</strong>:
                        <span th:text="${comment.content}">Comment content</span>
                    </li>
                </ul>
            </div>
        </div>
    </main>

    <footer th:replace="fragments/footer :: footer"></footer>
</body>
</html>
